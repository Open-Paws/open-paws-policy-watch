/**
 * Root app component.
 * Next.js routing handles page navigation — this file provides shared
 * providers and global layout when needed outside the pages/ directory.
 */
export default function App() {
  return null;
}
